# Copy to .env and fill ONE of these (or set them as RunCloud env variables instead).
# Hash is preferred. Generate with:
#   php -r "echo password_hash('YOUR_PASSWORD', PASSWORD_DEFAULT), PHP_EOL;"
MANI_PW_HASH=
# Or, simpler, a plaintext shared password:
# MANI_PW=MANI2026
